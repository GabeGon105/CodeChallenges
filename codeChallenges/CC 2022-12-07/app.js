// Write reverseList function that simply reverses lists.



// Parameters: an array, may be empty

// Return: the argument array reversed

// Examples:
reverseList([]) // []
reverseList([1,2,3]) // [3,2,1]



const reverseList = (list) => {
    // reverse the argument list and return
    return list.reverse();
}